<h1><%= link.keyword %> Link</h1>
<p>
  <b>URL:</b>
  <a href='<%= link.url %>'><%= link.url %></a>
</p>

<p>
  <b>Shortcut:</b>
  <% middle = port == 80 ? "" : ":#{port}" %>
  <% shortcut = "#{host}#{middle}/#{link.keyword}"  %>
  <a href='<%= "http://#{shortcut}" %>'><%= shortcut %></a>
</p>

<p>
  <b>Hits:</b>
  <%= link.hits %>
</p>

<p>
  <b>Created On:</b>
  <%= link.created_on.strftime('%A, %B %e, %Y') %>
</p>
